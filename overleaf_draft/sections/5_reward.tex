\section{Reward and Curriculum Synthesis}
\label{sec:reward}

% TODO: Eureka, DrEureka, Text2Reward. LLM writes reward code; RL trains; reflection revises.
Here the LLM writes \emph{reward} code rather than policy code; an RL loop trains on it
and a reflection step revises it~\citep{eureka}. One feedback loop, but the artifact
shipped is still weights, not a reusable skill.
